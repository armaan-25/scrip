# Outcome verification live check

Trivial marker file, opened as a real PR solely to give `GithubPrOutcomeVerifier`
a real GitHub PR to check merge/CI status against instead of a fake `fetch`.
Safe to delete or merge.
