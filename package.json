{
  "name": "scrip",
  "version": "0.2.0",
  "description": "Scrip connects what a person authorizes an agent to buy with what the agent actually pays for and what was delivered.",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json && mkdir -p dist/src/pricing && cp src/pricing/model_price.json dist/src/pricing/model_price.json",
    "test": "vitest run",
    "demo:deterministic": "tsx demo/deterministic-authorization.ts",
    "demo:protect": "tsx demo/protect.ts",
    "visuals:protect": "tsx visuals/protect-server.ts"
  },
  "dependencies": {
    "@naturalpay/sdk": "^1.6.16",
    "chalk": "^5.6.2",
    "js-yaml": "^4.1.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/js-yaml": "^4.0.9",
    "@types/node": "^22.9.0",
    "tsx": "^4.19.2",
    "typescript": "^5.6.3",
    "vitest": "^2.1.4"
  }
}
